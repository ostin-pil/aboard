# Audit 2026-08 — findings and session plans

The 2026-08-13 audit round, run against `main` at `708c934` (clean tree,
sessions 55 and 56 merged, no open PRs). Method: four parallel read-only
agents over status and roadmap, gaps and debt, security and performance,
and content and distribution. Every finding below was verified at a file
and line on that commit, not carried forward from an earlier sweep; the
two facts the agents disagreed on (earliest resolution date, README table)
were re-checked by hand before this document was written.

Relationship to `code-quality-audit.md`: that file stays the tracker for
the A–E findings and their status, and nothing here restates its rows.
This document records what the 2026-08 round found beyond them — five new
families (S security, P performance, U untracked debt, M distribution,
R records) — and then cuts the actionable backlog, carried E-items
included, into nine session-sized chunks with a plan each. A session that
takes a chunk closes its E-numbered items in `code-quality-audit.md`'s
status block as usual and its S/P/U/M/R items here.

## Verdict

The artifact is in the best shape it has ever been, and nobody is using
it. Engineering quality is high: a ten-command gate, 584 tests, every
E1–E9 finding closed, the graph component split into tested pure modules.
The diagnosis the project wrote about itself on 2026-07-22 ("the binding
constraint has flipped from engineering to distribution",
`research/reflection-2026-07.md`) has widened since: sessions 40–56 were
almost entirely refactors and gate ratchets while the distribution
backlog was copied verbatim into every log and never picked up. Total
measurable external consumption is zero (Smithery `useCount: 0` is the
only hard number in existence), and there is no instrumentation that
would detect it becoming nonzero. The chunks below are ordered to fix
that: make the front door truthful, add measurement, then promote — with
the reserved engineering items interleaved rather than displaced.

## Findings — S: security

| # | Sev | Finding |
|---|---|---|
| S1 | ~~MED~~ **CLOSED** | Unescaped `JSON.stringify` into `<script type="application/ld+json">` via `dangerouslySetInnerHTML` — `src/app/claims/[id]/page.tsx:88` and `:94`, same pattern at `src/app/dossiers/[claimId]/page.tsx:64` and `:70`. `JSON.stringify` does not escape `<`; a literal `</script>` in any agent-proposed string field (statement, title, source label/excerpt/finding, dossier theses) terminates the block and the remainder becomes live HTML in the static export. Reachable through `POST /api/proposals`: `LIMITS` bounds length, not characters. PR-gated by a human, but that reviewer is exactly the gate `pr-body.ts` was hardened to protect, and this is the same threat one layer down. Verified unexploited today (`out/claims/L3.html` contains no raw `<` in the block). Fix is escaping `<` as `\u003c` at the embed site. |
| S2 | ~~LOW~~ **CLOSED** | `MAX_PROPOSAL_BYTES` checks only declared `content-length` (`worker/index.ts:836-848`); `readEnvelope()` fully buffers and `request.json()`s the body (`:852`, `:887`) before Zod runs (`:857`). The comment's claim that "the schema bounds are the whole defence" does not hold: Zod caps what is committed, not what is buffered. Authenticated callers only, bounded by the limiter, so low. |
| S3 | LOW | Rate limiter fails open in both directions (`src/lib/rate-limit.ts:30-35`): missing binding or a thrown `limit()` both return allow. Deliberate and documented; flagged so the tradeoff stays explicit. Same shape on `/oauth/register` (`worker/oauth.ts:513`), where the limiter is the only brake between an anonymous script and 90 days of KV junk. Accepted; record, do not fix. |
| S4 | ~~LOW~~ **CLOSED** | `check:config` mirrors only one of the two rate-limit periods: `src/lib/config-lint.ts:321-346` matches `PROPOSAL_LIMITER` only. `REGISTRATION_LIMIT_PERIOD_S` (`worker/oauth.ts:64`) has the identical mirror obligation against `REGISTRATION_LIMITER` in `wrangler.jsonc` and is checked by nothing (both are 60 today, so no live drift). The lint also does not assert the platform constraint `period ∈ {10, 60}`, so `period: 30` would pass CI and fail at deploy. |
| S5 | ~~LOW~~ **CLOSED** | The 500 handler returns `error.message` to the caller (`worker/index.ts:949-953`), justified at `:946-948` by "every caller is authenticated". False for this handler: `route()` also serves anonymous `/mcp` reads, `/api/whoami`, markdown negotiation and every static asset. Repo is public so impact is low, but the stated justification does not cover the reachable set. |
| S6 | ~~INFO~~ **CLOSED** | No baseline security headers on the site: `public/_headers` sets content types, CORS and `Vary` only — no `X-Content-Type-Options`, `Referrer-Policy`, HSTS or CSP. The OAuth consent page sets its own strict set (`worker/oauth.ts:103-110`) and is the strictest surface in the codebase. A CSP would also have contained S1. |
| S7 | INFO | `/mcp` CORS allows the opaque `null` origin and any localhost origin regardless of port (`src/lib/mcp/protocol.ts:525-537`). Sound today because the endpoint sets no cookies and writes need a Bearer token, i.e. there is no ambient authority to ride; noted because the reasoning depends on that invariant holding forever. Accepted; record. |

Closed by session 59 (chunk 2), each verified by planting the fault and
watching the new check name it:

| # | Session | Closure |
|---|---|---|
| S1 | 59 | `ldJsonScript()` in `src/lib/jsonld.ts` escapes `<` before both embeds; four tests in `jsonld.test.ts`, and all 30 blocks in a built `out/` parse with no raw `<`. API routes keep bare stringify, so `v0.json` is untouched |
| S2 | 59 | `readCappedJson()` counts bytes while buffering and cancels the stream past the cap, returning 413. It lives beside `LIMITS` in `src/lib/proposals.ts` rather than the Worker, because vitest reads `src/` only; six tests, all on chunked bodies with no `content-length` |
| S4 | 59 | Both limiters check off one `LIMITER_MIRRORS` table that `scripts/check-config.ts` drives its extraction from, plus the `period ∈ {10, 60}` assertion the lint never made |
| S5 | 59 | The 500 path returns a fixed `"Internal error."`; the structured code stays. The old justification named a caller set that was never the reachable one |
| S6 | 59 | `nosniff`, `Referrer-Policy` and HSTS on `/*` in `public/_headers`. CSP deliberately deferred: the export inlines its bootstrap scripts, so a useful policy needs per-build hashes |

S3 and S7 stand as accepted, with the reasons above.

Also on the record: `key.pem` (P-384 registry signing key) sits at repo
root — untracked, gitignored (`.gitignore` `*.pem`), mode 600, never in
history — but `scripts/publish-registry.sh:112` prefers that on-disk PEM
over the keychain source it already supports at `:129`. Inverting the
order costs nothing and retires the "move key.pem out of the repo" item
carried since session 33. **Session 59 inverted the order**; storing the
key in the keychain and deleting the file are operator steps, and until
they happen the PEM is still what signs.

Clean, checked and found nothing: committed or historical secrets (grep
over tree and full history), OAuth flow (PKCE, RFC 8707 audience, sealed
consent state, allowlist at issuance, RFC 9207 `iss`), PR-body forging
(`pr-body.ts` fence containment), YAML injection (`serialize.ts` uses
`YAML.Document`), path traversal into the GitHub API (every path segment
server-derived or validated), branch-name injection, static token table
handling (fails closed, prototype-safe), `WWW-Authenticate` injection,
markdown-negotiation traversal, CI workflow (`pull_request`, pinned
actions, no secrets).

npm audit (nothing installed, all three roots have `node_modules`):

| Root | crit | high | mod | low | Reachability |
|---|---|---|---|---|---|
| `/` | 0 | 6 | 0 | 1 | None from the Worker or static export; all build-time or dev tooling (`next` middleware CVE needs middleware; `sharp`/`postcss`/`js-yaml` are build-time) |
| `mcp-server/` | 0 | 3 | 1 | 2 | Transitive via the MCP SDK's HTTP transport; this server runs stdio-only |
| `clients/` | 0 | 1 | 0 | 1 | Dev tooling (`esbuild` via `tsx`) |

## Findings — P: performance

| # | Sev | Finding |
|---|---|---|
| P1 | HIGH | The homepage ships the entire React Flow engine: 11 chunks, 1,132,282 bytes raw / 320,634 gzip, measured against `out/index.html`. `src/app/page.tsx:6` statically imports `ClaimGraphCanvas`, which statically imports `ClaimGraphRF` and `@xyflow/react` plus its stylesheet. No `next/dynamic` or `React.lazy` exists anywhere in `src/`. The xyflow chunk alone is 208,662 bytes raw, on the critical path of an inline graph that is deliberately near-inert (`panOnDrag={false}` etc.). `ClaimGraphCanvas` already gates the subtree behind a `mounted` flag with a loading placeholder, so `next/dynamic({ ssr: false })` is a free ~200 KB off the initial payload. |
| P2 | MED | Zod v4 is in the client bundle to validate localStorage: `src/components/graph/persist.ts:1` imports `z`, pulling a 286,621-byte chunk (~65 KB gzip, the largest on the homepage) to check a self-authored, single-origin snapshot. A hand-written guard removes it. The same dependency set is materialized twice — a second, byte-different 286,621-byte chunk serves only `out/graph.html`. |
| P3 | LOW | The canvas itself is well optimized (hoisted `NODE_TYPES`/`EDGE_TYPES`, memoized nodes/edges/context, `onlyRenderVisibleElements`, refs-mirror pattern). Two quadratic-shaped spots are harmless at n=25 and worth knowing: `isNeighbor` scans all edges per node per focus change (`ClaimGraphRF.tsx:194-205`), and the `useLayoutEffect` resize handler calls `rf.fitView()` unthrottled on every resize event (`:513-517`). |
| P4 | LOW | Worker does uncached per-request work: `run_worker_first` puts every page request through the Worker to read one `Accept` header (deliberate, scoped correctly); `readGraph` re-fetches and re-parses the 148 KB `/api/graph` asset on every proposal, and MCP `get_graph`/`list_claims` do the same on every anonymous read with no cache and no read rate limit — no `caches.default` anywhere in `worker/`. `claimIdsByDomain` builds with array spread in a loop (`worker/index.ts:269`), quadratic per domain, a `.push` away from linear. All bounded at current scale. |
| P5 | INFO | `out/` totals 8.8 MB across 440 files; heaviest items are the two 286 KB zod chunks, react-dom (227 KB), xyflow (208 KB), `out/api/graph` (148 KB), `out/claims/L3.html` (112 KB of HTML that also pulls 637 KB of JS). CSS is 82 KB across two files, driven by the unconditional xyflow stylesheet import — it follows the graph into a dynamic chunk if P1 lands. |

## Findings — U: untracked gaps and debt

| # | Sev | Finding |
|---|---|---|
| U1 | SIG | `vitest.config.ts:15` (`include: ["src/**/*.test.ts"]`) structurally excludes every non-`src/` surface: a test written under `worker/`, `mcp-server/` or `clients/` would not run. Unreachable by the gate: `worker/index.ts` (1023 lines — the write path, PR minting, rate limiting), `worker/oauth.ts`, `worker/mcp.ts`, all of `mcp-server/src` (724 lines), `clients/` (530 lines). The config comment's `server-only` justification does not apply to any of those, which are pure HTTP code. The audit doc already calls a Worker test surface "the single highest-value gap"; that thread fell out of session next-steps after 48. |
| U2 | SIG | Two independent MCP tool surfaces with no cross-check: `mcp-server/src/tools/{read,write}.ts` (stdio, zero tests) and `src/lib/mcp/` (remote, tested) both define the tools by hand. The repo already owns the right pattern (`enum-sync.test.ts`, `server-card.test.ts` holding four version homes in agreement) and has not applied it here; schema drift between the two servers fails nothing. |
| U3 | SIG | Zod major-version skew: root `zod@^4.4.3`, `mcp-server` `zod@^3.23.8`, with the security-relevant `HttpUrl` constraint hand-mirrored across the boundary (`mcp-server/src/tools/write.ts:35-47`) in the package with no tests. A shared accept/reject vector set run through both schemas would pin it. |
| U4 | MIN | No dependency gate of any kind: no `npm audit` step in CI, no `.github/dependabot.yml`. Named in the audit's C list for five sessions, never scoped. |
| U5 | MIN | Untested-module map beyond U1, all inside `src/`: `ClaimGraphRF.tsx` (640) and the three `use-*.ts` hooks (named untested in CLAUDE.md), `src/lib/data/exporter.ts` (where E12 lives), `serialize.ts` (E15's guard), `loader.ts`'s walker (E16), `align.ts`, `jsonld-export.ts`, `GraphContext.tsx`, and every `.tsx` component and route — no component test, no jsx-a11y, no axe. The shape is deliberate (pure arithmetic tested, wiring not); exporter and serialize are pure and could be tested today. |
| U6 | MIN | The documented browser-verification path for that untested surface does not work unattended: an automated tab reports `visibilityState === "hidden"`, never fires rAF, so nothing persists and nothing renders (CLAUDE.md); whether `reset` also freezes a *painting* tab is session 55's open question, promised into `knowledge/issues.md` and never filed (R7). |
| U7 | MIN | The server version has four hand-written homes held together by `src/lib/mcp/server-card.test.ts` — the right mitigation, but the duplication (and the two-spelling server-card hedge from session 30) has never been revisited. Accepted; record. |

## Findings — M: distribution, content, consumption

| # | Finding |
|---|---|
| M1 | No telemetry exists anywhere: no `observability` block, no Analytics Engine binding, no Logpush in `wrangler.jsonc`, no `writeDataPoint` or request logging in `worker/`, no client-side analytics. The only measurement ever taken was AI Crawl Control showing zeros with 24-hour retention (`sessions/2026-07-25_session_27.md:79-88`). The owner has no way to know whether anyone reads the site or calls the API. |
| M2 | The launch post (`agent-distribution.md` §5, one LessWrong/EA Forum post) is unwritten. It was gated on OAuth; OAuth shipped 2026-07-27; it has not moved in the 17 days since. |
| M3 | The §2 registry listings — Glama, mcp.so, `punkpeye/awesome-mcp-servers` PR — are unexecuted, carried in session next-steps since 33. What does exist and was verified live during this audit: the official registry entry (`me.untype/aboard` 0.1.0, active, published 2026-07-29, remote pointing at `https://aboard.untype.me/mcp`) and the Smithery listing (deployed, description set, `useCount: 0`, unverified). |
| M4 | Nothing is npm-installable: all three `package.json`s are `private: true` with no `publishConfig`; both `aboard-mcp-server` and `aboard-clients` 404 on the registry; the mcp-server `bin` entry points at raw TypeScript and works only via its `npx tsx` shebang. The distribution story (registry card, README) points at a package nobody can install. |
| M5 | The GitHub repo (public since 2026-05-08) has an empty description, empty homepage URL, 0 stars, 0 forks. The one free distribution surface costs five minutes and is unfilled. |
| M6 | Zero funding applications submitted. `funding-applications.md` lists three "apply now" targets with no dated status line, and all six §3 evidence-checklist boxes are unchecked, including "one external consumer" and "one citation or mention". |
| M7 | The FLF competition outcome is untracked: submitted by the 2026-07-19 deadline (recorded three weeks late, session 53), no confirmation reference, no result on record. |
| M8 | Corpus shape: 25 claims / 12 forecasts / 5 dossiers / 25 intra + 3 cross-domain edges over 3 domains. Quality is real (steel-manned dossiers with ranked cruxes and real PMIDs/DOIs; 11 of 12 forecasts carry a structured `resolutionSource`). Thin spots: `epistack_cases` is a disconnected island (4 claims, 0 forecasts, no cross-domain edge touches any `EC*` id — against the project's own cross-domain-by-design premise); dossier coverage is 5 of 25 claims; the analyses module is n=1; the `evidences` edge kind has never appeared in `data/` (0 hits), so session 49's fix remains test-proven only. |
| M9 | No forecast has resolved and none can before **2027-01-31** (F9, the earliest `resolutionDate`; the full ladder runs F9, IF2, then four at 2027-03-31). The "first resolved forecast + Brier score" milestone the funding plan and the §5 leaderboard depend on is calendar-blocked, not work-blocked, and should be recorded as such wherever it appears in a prerequisite column. |

## Findings — R: records that no longer tell the truth

| # | Finding |
|---|---|
| R1 | `README.md` State table is wrong on every row but one: says 2 domains (3), 20 claims (25), "5 ensembles + IF1" (12 forecasts), 1 dossier (5). |
| R2 | The README's headline forecast — "the finding aboard exists to demonstrate" (`README.md:5-14`, repeated at `content/about.md:88`) — is F4, which `data/.../forecasts/F4.yaml:12` marks `supersededBy: [F7]` for unanchored resolution criteria. F5 is likewise superseded by F6+F8. The project's pitch currently rests on a forecast the project itself retired. |
| R3 | `README.md:108` and `research/agent-onboarding.md:39` advertise a `search_claims` tool that exists in neither MCP surface. The same defect was found and fixed for the about page (`proposed-direction-2026-07.md:61-65`) and nowhere else. |
| R4 | `supersededBy` exists in the schema, serializer, integrity checks and lint — and in zero `.tsx` files. F4 and F5 render to human readers as live forecasts. The UI marker has been carried since session 38. |
| R5 | `plans/README.md` still lists `corpus-growth.md` and `agent-surface.md` as open work that has substantially shipped, and session 56's next-steps item 8 re-carried a plans/README defect session 53 had already fixed — the carry-forward mechanism copies faithfully and verifies nothing. |
| R6 | `code-quality-audit.md`'s E21 row is half-stale: the robots half is closed (deliberate static `public/robots.txt` with the allow stance); verify the `alternates.canonical` half in-session before touching the row. |
| R7 | `knowledge/issues.md` has had no new entry since 2026-08-05 (~session 41) while ~15 live findings accumulated in `code-quality-audit.md`; two bugs session 55 explicitly promised into it — the `reset` freeze question and undo not refreshing the chrome's counts (`sessions/2026-08-12_session_55.md:249-254`) — were never filed. Which file is the tracker is nowhere stated. |
| R8 | `knowledge/test-cases-graph.md`, the one written procedure for the untested canvas surface, anchors to pre-split code ("the `useMemo` in `ClaimGraphRFInner`, around line 97"); the decision it targets moved to `seed.ts` in session 55. |

Process observations, for whoever runs the next round: the what's-next
mechanism carries items without verifying them (R5); three session-38
threads (superseded marker, dossier source-characterisation pass,
identity-machinery plan) vanished from the lists after session 51 without
a recorded decision; the worker-test thread did the same after session 48.
`tokens.ts` has been promised in five consecutive logs, the `evidences`
edge named in six, and the stale-edge-id item reserved and unclaimed for
six. Chunks below pick all of these up or record their disposition.

## Session plans

Nine chunks, each sized to one session by the scope recent sessions have
actually absorbed (session 54 closed five findings; session 55 split a
1207-line component). Chunks are independent unless a dependency is
named. Suggested order at the end.

### Chunk 1 — the truth batch

Closes R1–R8, M5, M9's bookkeeping, and the session-38 marker carry (R4).
Everything here is cheap, and promotion (chunks 7–8) multiplies whatever
the front door says, so this goes first.

- `README.md`: fix the State table (3 domains, 25 claims, 12 forecasts,
  5 dossiers, 3 cross-domain edges); re-derive or drop the spread row.
- Re-frame the headline: F7 is F4's named replacement. Keep F4 as the
  historical spread/outlier story only with its superseded status stated,
  in both `README.md:5-14` and `content/about.md:88`.
- Delete `search_claims` from `README.md:108` and
  `research/agent-onboarding.md:39`; list the nine real tools.
- Superseded-forecast marker: a small `SupersededNotice` component
  rendering the status and linking the replacements via `supersededBy`,
  used wherever forecasts render. Keep it out of
  `claims/[id]/page.tsx`'s body — that file is at 419 lines against the
  250 rule and must not grow.
- GitHub metadata: `gh api repos/{owner}/{repo} -X PATCH
  -f description="..." -f homepage="https://aboard.untype.me"`.
- `plans/README.md`: move `corpus-growth.md` and `agent-surface.md` to
  Shipped with honest residue notes; mark "one resolved forecast" in the
  funding row as calendar-blocked until 2027-01-31 (M9).
- `code-quality-audit.md`: correct E21's robots half; verify the
  canonical half before touching it (R6).
- `knowledge/issues.md`: file the two session-55 bugs; add a header line
  stating the tracker split (issues.md for runtime/dev issues,
  code-quality-audit.md for audit-finding status, this file for S/P/U/M/R).
- `knowledge/test-cases-graph.md`: re-anchor TC steps to the post-split
  modules (`seed.ts` et al).

Verify: `npx tsc --noEmit`, `npm run build`, `npm test` (component
change), `npm run lint`; read the GitHub metadata back. Exit: nothing the
front door or the trackers state is false. Effort: about half a day.

### Chunk 2 — security batch — DONE (session 59)

Closes S1, S2, S4, S5, S6 and the key.pem resolution order; records S3
and S7 as accepted with their reasons (done by this document; the chunk
re-affirms in commit messages if it touches those files).

Landed as written, with three departures worth carrying forward. The S1
helper has two call sites, not the three this plan claimed: the four line
references in the finding are the `JSON.stringify` call and the
`dangerouslySetInnerHTML` line of each of two files. S2's cap went into
`src/lib/proposals.ts` instead of the Worker, because `worker/` is
outside vitest's include and the Worker home would have shipped it
untested; chunk 6 is what would change that. And the CSP probe was not
run, so S6 closed on the three easy headers exactly as the plan allowed.

- S1: escape `<` as `\u003c` in the ld+json embeds. Put the
  stringify-and-escape in one helper (e.g. `ldJsonScript()` beside the
  serializers) used by all three sites, with a unit test running a
  statement containing `</script>` through it. The API output is
  unchanged — this is the embed layer, so `v0.json` is untouched.
- S2: read the body through an explicit cap (count bytes while buffering,
  abort past `MAX_PROPOSAL_BYTES`) or, at minimum, correct the comment at
  `worker/index.ts:836-848` so nobody relies on its claim.
- S4: extend `src/lib/config-lint.ts` to mirror `REGISTRATION_LIMITER`
  against `worker/oauth.ts`'s period and to assert `period ∈ {10, 60}`
  for both limiters; tests beside the existing config-lint suite.
- S5: return a generic message on the 500 path; keep the structured code.
  Server-side visibility arrives with chunk 4.
- S6: add `X-Content-Type-Options: nosniff`, `Referrer-Policy` and HSTS
  to `public/_headers`. Probe a CSP separately — Next's static export
  inlines scripts, so hashes may fight back; do not let the probe block
  the three easy headers.
- Invert `scripts/publish-registry.sh` key resolution (keychain first,
  PEM fallback), store the key in the keychain, delete `key.pem`;
  `shellcheck` after.

Verify: `npx tsc --noEmit`, `npm run build`, `npm test`,
`npm run check:config`, `shellcheck`; after the next deploy, `curl -I`
for the headers. Effort: about half a day.

### Chunk 3 — bundle diet and graph QA

Closes P1, P2; answers U6's open question; optional riders from P3/P4.

- P1: wrap the `ClaimGraphRF` import in `next/dynamic({ ssr: false })`
  inside `ClaimGraphCanvas` — the mounted-gate and loading placeholder
  already exist, so behaviour is unchanged. The xyflow stylesheet follows
  the graph into the dynamic chunk.
- P2: replace the Zod parse in `src/components/graph/persist.ts` with a
  hand-written guard over the persisted shape; extend `persist.test.ts`
  with the malformed-input vectors the schema used to catch.
- Measure before/after: total raw/gzip for the homepage chunk set
  (baseline 1,132,282 / 320,634) and record the delta in the session log.
- Browser pass against `npm run build` output (rAF-patch caveat in
  CLAUDE.md). While there, answer session 55's question: does `reset`
  freeze a *painting* tab? File the answer in `knowledge/issues.md`
  either way.
- Riders if time allows: debounce the `fitView` resize handler; the
  `.push` fix at `worker/index.ts:269`. Leave `caches.default` for
  chunk 6, where the worker gets tests.

Verify: `npx tsc --noEmit`, `npm run build`, `npm test`,
`npm run check:built-urls`, the browser pass. Effort: about half a day.

### Chunk 4 — instrumentation

Closes M1. Must precede the launch post: promoting an unmeasured surface
wastes the one launch.

- Add an Analytics Engine dataset binding to `wrangler.jsonc` and one
  `writeDataPoint` per event in the Worker: proposal POST (outcome
  class), MCP call (tool name, anonymous vs authenticated), markdown-twin
  negotiation hit. Low cardinality, no tokens, no PII.
- Optionally enable the `observability` logs block while in the file.
- `npm run check:config` after every `wrangler.jsonc` edit — it is the
  only local reader; extend `config-lint` only if the binding needs
  pinning.
- A short how-to-query note (the SQL API, free-plan retention) in
  `worker/README.md`.

Verify: `npm run check:config`; CI's dry-run covers the bundle; after
deploy, one curl followed by one SQL query returning the row. Exit: "did
anyone call the API this week" is answerable. Effort: about half a day.

### Chunk 5 — exporter and edge identity

The reserved item, unclaimed for six sessions. Closes E12, the stale
edge id, and E15's duplicate-relation refusal; riders E16 and E17. This
chunk also converts two untested pure modules (U5) into tested ones.

- E12: stop re-minting edge ids from `E1`. The exporter's graph already
  carries canonical ids for seeded edges; preserve them, mint new ids
  continuing after the domain's maximum, and emit `edges.yaml` as the
  merged set so "drop the files into `data/`" stops deleting live edges.
- Stale edge id: extend session 51's `409 id_collision` machinery to
  edges, so a stale snapshot fails at submit with remediation rather
  than at CI on the PR.
- Duplicate-relation refusal (E15's sharpest tooth): refuse a proposed
  edge whose (from, to, kind) already exists.
- New suites: `exporter.test.ts` and `serialize.test.ts` (the `"[]"`
  comment guard from E15 gets a case).
- Riders: E16 — sort `readdirSync` results in `loader.ts` so `/api/graph`
  arrays are reproducible across filesystems, one line plus a test; E17 —
  `Forecast.parse` before `ensemble-predict --update` writes.

Verify: `npm test`, `npm run build`; the worker-side edge-collision path
gets its automated coverage when chunk 6 lands (running 6 first makes
this chunk fully testable, but do not let that gate it — a curl check
against a preview deploy suffices). Effort: about a day.

### Chunk 6 — worker and MCP test surface

Closes U1, U2, U3; riders E13, E14, E18. Decision-heavy.

- Decide the harness: `@cloudflare/vitest-pool-workers` as a second
  vitest project (probe compatibility with vitest 4 first) versus a
  plain node project with mocked bindings. The audit doc has called this
  the highest-value gap since v2.
- Restructure `vitest.config.ts` into projects so the `src/` suite is
  untouched; wire the new suite into `npm test` so the existing gate
  covers it, and run `npm run check:config` if the gate or CI lists
  change.
- First tests, chosen to pin current behaviour and then fix two
  findings against them: auth and limiter ordering (E14 — decide the
  order deliberately, then encode it), `MAX_PROPOSAL_BYTES`, branch
  cleanup on failure paths (E13 — delete the ref the 409 path orphans),
  markdown negotiation, anonymous MCP reads.
- U2/U3: a parity test pinning the stdio tool surface to the remote one
  (names, input schemas, and a shared accept/reject vector set run
  through both `HttpUrl` implementations). Placement will fight the
  tsconfig boundaries; if it lands inside `mcp-server/`, add the run
  command to the gate and CI and settle `check:config`.
- Rider E18: default `mcp-server/src/http.ts` base URL to
  `https://aboard.untype.me`, where `/api/proposals` exists.

Verify: `npm test`, `npm run typecheck:mcp`, `npm run check:config`, CI
green. Effort: about a day.

### Chunk 7 — distribution mechanics

Closes M3, M4, and the mechanical rest of `agent-distribution.md` §2.
Operator-heavy; run after chunk 1 so every listing points at a truthful
front door.

- Listings: Glama and mcp.so submissions; the awesome-mcp-servers PR.
- npm: give `mcp-server/` a build step emitting JS, point `bin` at the
  build, drop `private: true`, add `publishConfig`, publish
  `aboard-mcp-server`; decide `clients/`' fate (publish or fold into
  docs) rather than leaving it half-declared.
- Update the README and server-card install instructions to the real
  `npx aboard-mcp-server` path once it exists.
- Search Console: verify the domain, submit the sitemap.

Verify: `npx aboard-mcp-server` from a clean directory against the live
site; `npm run typecheck:mcp`; listings visibly live. Effort: half a day
to a day, mostly operator time.

### Chunk 8 — the launch post

Closes M2, per `agent-distribution.md` §5: one post, LessWrong or EA
Forum. Depends on chunk 1 (the story must not lead with a retired
forecast), chunk 4 (so the effect is measurable), and ideally chunk 7
(so `npx` works when a reader tries it).

- Draft in-repo (`research/` or beside the plan) so the prose gate can
  read it; the framing is the methodology-first one the reflection
  argues — both-readings-as-tension, F4 as the spread/outlier story told
  honestly including its supersession, the agent-write path as the
  differentiator.
- `/prose-check` the draft; post; add link-backs where natural (about
  page, README).
- Watch the chunk-4 instrumentation over the following week and record
  the numbers, whatever they are, in the session log.

Effort: about half a day of writing plus operator posting.

### Chunk 9 — carried small items

Closes the two oldest promises and the a11y gap: `tokens.ts` (promised
five consecutive sessions), the first `evidences` edge (named in six),
jsx-a11y + axe (named in three). Riders E19 and the E20 decision.

- `src/lib/tokens.ts`: the 15 distinct colors behind the 59 re-typed hex
  values; the three OG images consume it. Rider E19: while in token
  land, collapse the duplicated dark block in `globals.css` to one
  source. `npm run check:exports` after — new exports must have
  importers or knip fails the gate.
- The first `evidences` edge in `data/`: pick a real evidential link
  (a dataPoint-backed claim supporting a mechanism), add it with
  rationale and source. `npm run build` is the data gate; this also
  exercises session 53's `*.yaml` classification and turns session 49's
  fix from test-proven into data-proven.
- a11y: `eslint-plugin-jsx-a11y` into the eslint config (fix what it
  flags — do not add warnings while `--max-warnings 0` is a live
  ratchet target), plus an axe-core check over the three modals under
  jsdom (installed in session 54 for exactly this).
- E20 decision if energy remains: Tailwind is imported with zero utility
  classes used anywhere — adopt or drop, and record which.

Verify: `npx tsc --noEmit`, `npm run build`, `npm test`, `npm run lint`,
`npm run check:exports`. Effort: about half a day.

## Left unchunked, with reasons

- The C ratchets (`--max-warnings 0`, `noUncheckedIndexedAccess`,
  coverage thresholds, dependency gate incl. U4): 7 of the 13 warnings
  live in `Claude Design Screens/`, so the ignore-versus-delete decision
  on that directory comes first and is the operator's.
- M9 / the first resolved forecast: calendar-blocked until 2027-01-31.
  Chunk 1 records it as such; nothing else to do.
- M6 funding applications: operator writing, unblocked further by
  chunks 1 and 4 (truthful evidence, a consumption number).
- The identity-machinery plan (session 38): write it when the work is
  picked up, per `plans/README.md`'s standing note.
- `claims/[id]/page.tsx` at 419 lines against the 250 rule: standalone
  split, or ride any chunk that touches the file; chunk 1 deliberately
  avoids growing it.
- U7 (version homes) and S3/S7: accepted, test-held or documented;
  recorded above.

## Suggested order

1 (truth) → 2 (security) → 4 (instrumentation) → 3 (bundle) →
7 (mechanics) → 8 (post), with 5, 6 and 9 interleaving freely — 6 before
5 makes 5's worker half testable, but 5 has waited six sessions and
should not wait on another decision. The only hard dependencies are
chunk 8's on 1 and 4.
